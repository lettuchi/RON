Script backups for Ryoko Owari — NOT loaded by Ren'Py.

Ren'Py recursively loads every file ending in .rpy (and compiled .rpyc) under game/.
Do NOT store backups here with a .rpy or .rpyc extension.

Use .rpy.bak / .rpyc.bak (or .txt) instead. Folder names are dated snapshots for human reference only.
