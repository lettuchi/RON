# Fix pass (2026-06-04)

## Fixed

### 1) CG `cg-case5-oath-seal-hand-closeup.png`
- Backed up prior PNG to `game/images/cg/versions/case5-oath-seal-hand-closeup-backup-2026-06-04-fixpass.png`.
- Regenerated with Cursor `GenerateImage` (refs: `toa-neutral.png`, `kaoru-smirk.png`, prior backup for composition).
- Prompt emphasis: pale slender long elegant Crane-dancer fingers, neat nails, black kosode cuff and gold crane obi hint; Kaoru tanned hand secondary over red wax seal.
- Installed to `game/images/cg/cg-case5-oath-seal-hand-closeup.png`.
- Prior MediaPipe hand QA on old file: PASS (1 hand). Re-run optional after visual review.

### 2) Voice gaps
- **Case 1 investigation main path** (`case1_registry_start` through barge): wired **65** character lines that lacked `voice` (Plum registry, Chrysanthemum, barge beats).
- New IDs: **`toa_338`–`toa_364`** (27), **`kaoru_449`–`kaoru_486`** (38). ID list: `scripts/case1_investigation_main_voice_ids.txt`.
- Regenerated all 65 with `eleven_v3`; log: `scripts/case1_investigation_main_voice_regen_2026-06-04.log`.
- Promoted v2 → legacy; log: `scripts/case1_investigation_main_voice_promote_2026-06-04.log` (65 promoted, 0 skipped).
- **`toa_312`**: intentionally unused (script `toa_311` → `toa_313`). Removed from `scripts/staged_regen_stage1_case1_5_ids.txt` (was never in voice manifests).
- **Case Zero**: spot-check **93/93** wired IDs, **0** missing legacy MP3s (`scripts/_case_zero_spotcheck.py`).
- **UTF-8 BOM**: rewrote `scripts/case1_merge_voice_ids.txt` and `scripts/voice_coverage_missing_ids.txt` as UTF-8 no BOM.

### 3) Script hygiene headers
- `game/case1_5_romance_interlude.rpy`: updated VOICED comment (wired ranges; notes no `toa_312`).
- `game/kaoru_pro_prologue.rpy`: `kaoru_387-430` → **`387-425`**, legacy MP3s note.
- `game/case5_investigation.rpy`, `game/epilogue_romance_bonus.rpy`: removed stale "regen batch TBD".
- `game/case1_investigation.rpy`: replaced "no voice lines" NOTE with VOICED range pointer.

### 4) Gallery / Case Zero
- Verified `game/gallery.rpy`: **`Extra: Case Zero (Pro-prologue)`** group with **2** entries (`case_zero_redacted_docket`, `case_zero_rain_alley_door`). No change needed.

### 5) Quick verify
- Wired voice IDs: **1167**; missing legacy `game/audio/voice/{id}.mp3`: **0**.
- Case 1 companion + investigation unwired dialogue scan: **0**.
- `read_lints` on edited `.rpy`: **no issues**.

## Still open / deferred
- **Menu choice labels** (~155 across project): still unvoiced by design (Ren'Py default).
- **`epilogue_kotatsu_warmth` closeup**: still stubbed per `docs/cg-closeup-intimate-2026-06-04.md` (NovelAI / GenerateImage policy).
- **Manifest-only high IDs** (case5 header band not wired in `.rpy`): report-only; no MP3 regen unless future wiring.
- **Visual QA** on new case5 hand CG: human pass recommended (automation cannot judge "delicate" vs prior).

## Commands run

```text
python scripts/wire_case1_investigation_main.py
python scripts/regenerate_voice_with_metadata.py --check
python scripts/regenerate_voice_with_metadata.py --model eleven_v3 --ids @scripts/case1_investigation_main_voice_ids.txt
python scripts/promote_v2_voice_ids.py --ids-file scripts/case1_investigation_main_voice_ids.txt --log scripts/case1_investigation_main_voice_promote_2026-06-04.log
python scripts/_voice_missing_count.py
python scripts/_case_zero_spotcheck.py
python scripts/_tmp_unwired.py
python scripts/check_cg_hands.py --path game/images/cg/cg-case5-oath-seal-hand-closeup.png
```

GenerateImage: case5 oath seal hand closeup (refs: toa-neutral, kaoru-smirk, backup PNG).

## New voice ID ranges (this pass)

| Character | Range | Count |
|-----------|-------|------:|
| Toa | `toa_338` – `toa_364` | 27 |
| Kaoru | `kaoru_449` – `kaoru_486` | 38 |

Helper script added: `scripts/wire_case1_investigation_main.py` (reusable for future gap scans).

No git commit (per request).
